# Python equivalents of the Node dependencies in the original package.json
#   express            -> flask
#   compression        -> flask-compress
#   body-parser        -> (built into flask)
#   cookie-parser      -> (built into flask)
#   express-http-proxy -> requests (see app/app_ex.py)
#   node-fetch         -> requests
#   jsonwebtoken       -> pyjwt
#   formidable         -> werkzeug (via flask)
#   jszip              -> zipfile (stdlib)
#   ip                 -> socket (stdlib)
Flask>=3.0.0
Flask-Compress>=1.14
PyJWT>=2.8.0
requests>=2.31.0
Werkzeug>=3.0.0
